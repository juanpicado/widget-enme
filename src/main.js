// require.config({

//   // Sets the js folder as the base directory for all future relative paths
//   baseUrl: "./js",

// });


require(["modules/componets/poll_form"], function(poll_form) {
    console.log("proo", poll_form);
});